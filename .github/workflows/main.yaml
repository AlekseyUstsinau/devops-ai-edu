name: CI/CD Pipeline

on:
  workflow_dispatch: {}

jobs:
  deploy:
    name: Build → Deploy → Quality Gate → Notify
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
